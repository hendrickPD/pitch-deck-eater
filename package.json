{
  "name": "pitch-deck-eater",
  "version": "1.0.0",
  "description": "A Slack bot that captures and converts Pitch/Miro links to PDFs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "npm install",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
